import type { FC, ReactElement } from 'react';
import { DetailProject } from './_components/detail-project';

export const DetailProjectPage: FC = (): ReactElement => {
  return <DetailProject />;
};
